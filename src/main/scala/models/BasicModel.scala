package models

import views.viewadapters.ViewAdapter

case class BasicModel(private val view: ViewAdapter) {
  def start = {

  }

  def createImages(numImages: Int, width: Int, height: Int): Unit = {

  }
}
